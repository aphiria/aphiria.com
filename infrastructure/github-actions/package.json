{
  "name": "@aphiria/github-actions-utilities",
  "version": "1.0.0",
  "description": "Lightweight utility functions for GitHub Actions workflows (retry, poll, validation)",
  "main": "bin/index.js",
  "types": "bin/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf bin",
    "rebuild": "npm run clean && npm run build"
  },
  "keywords": [
    "github-actions",
    "retry",
    "polling",
    "validation"
  ],
  "author": "Aphiria",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3"
  }
}
