/**
 * Shared infrastructure constants
 * Only contains values used across multiple components
 */

/** PostgreSQL standard port (used in database, api-deployment, db-migration) */
export const POSTGRES_PORT = 5432;
