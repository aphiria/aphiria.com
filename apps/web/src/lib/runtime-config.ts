/**
 * Runtime configuration loaded from /js/config/config.js
 *
 * This file is dynamically generated by Pulumi and mounted into the container.
 * It contains environment-specific values like API URI and cookie domain.
 *
 * The config.js file sets window.__RUNTIME_CONFIG__ at runtime.
 */

export interface RuntimeConfig {
    apiUri: string;
    cookieDomain: string;
}

// Extend Window interface for TypeScript
declare global {
    interface Window {
        __RUNTIME_CONFIG__?: RuntimeConfig;
    }
}

/**
 * Get runtime configuration (client-side only)
 *
 * Loaded from /js/config/config.js which is mounted by Kubernetes at deploy time.
 * This allows the same static build to work across environments.
 *
 * @returns Runtime configuration or development defaults
 */
export function getRuntimeConfig(): RuntimeConfig {
    // Server-side or config not loaded: return development defaults
    if (typeof window === "undefined" || !window.__RUNTIME_CONFIG__) {
        return {
            apiUri: "http://localhost:8080",
            cookieDomain: "localhost",
        };
    }

    return window.__RUNTIME_CONFIG__;
}
